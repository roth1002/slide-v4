// 把 store 傳進整個 react app 的 context 裡
// App.js
import { Provider } from 'react-redux';

const App = (
  <Provider store={store}>
    <MyRootComponent>
  </Provider>
);

ReactDOM.render(
  <App />,
  document.getElementById('root'),
);
